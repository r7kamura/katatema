#!/usr/bin/env node
import { resolve } from "path";
import build from "../lib/build";

const directoryPath = resolve("./docs");

build(directoryPath).catch((error) => {
  console.error(error);
  process.exit(1);
});
